{
  "name": "advanced-sample",
  "private": true,
  "scripts": {
    "dev": "bun src/index",
    "build-webapp": "tsc -b webapp && vite build webapp",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fcannizzaro/native-window": "workspace:*",
    "@fcannizzaro/native-window-ipc": "workspace:*",
    "@fcannizzaro/native-window-tsdb": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "vite-plugin-singlefile": "^2.3.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
