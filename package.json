{
  "name": "native-window-monorepo",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "packages/*",
    "samples/**/*"
  ],
  "scripts": {
    "changeset": "changeset",
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@types/bun": "^1.3.9",
    "oxfmt": "^0.34.0",
    "turbo": "^2.8.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
